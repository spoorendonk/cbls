# Curated root-cause notes for instances the runner cannot solve, merged into
# comparison.csv's note column by cbls_minlplib. Each row records whether the
# failure is a solver defect (with a tracking issue) or hardness of the instance
# for this method. Where a mechanism is stated it must be one the code supports;
# say so explicitly when it is not yet identified. Lines starting with '#' are ignored.
#
# classification: bug | hard
instance,classification,note
elec25,bug,"Thomson problem: the feasible region contains coincident-point configurations where the Coulomb objective is +inf. Folded in as an obj<=bound soft constraint, that violation clamps to ~1e30 and absorbs the real constraints' O(1) terms in floating point, so the search loses the feasibility signal. Verified: dropping the objective makes this instance feasible in 20s at violation 0. Tracked in #100."
elec50,bug,"Same mechanism as elec25, at 50 points; see #100."
nvs01,hard,"Requires changing x0 and the product x1*x2 together in 420.169*sqrt(x0^2+900) == x2*x0*x1. While x0=0 the product term vanishes so x1 and x2 get no gradient signal, and no single-variable jump improves; escaping needs a compound move (Novelty Jump is implemented but off by default). Tracked in #101."
st_e40,hard,"Structure: rows C1-C3 are degree-7 polynomials (x-1)(x-2)(x-3)(x-5)(x-8)(x-10)(x-12)==0 restricting each integer to {1,2,3,5,8,10,12}; C0 then pins the free x3 to a bilinear function of them and four linear rows bound the combination. The search reaches a point satisfying C1-C3 (the integers sit on allowed values) but missing a linear row by ~2. The precise local-optimum mechanism is NOT yet identified: an earlier note here claimed a violation barrier between allowed integer values, which is wrong - int_jump_candidates enumerates the whole domain when that domain spans at most 256 values, and these are [1,12], so any allowed value is one jump away. Tracked in #102."
