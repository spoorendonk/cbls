# AUDIT (#75 / 2026-05-04): rows below are NOT comparable across methods.
# - SCIP rows use a known-broken formulation: linear-in-v fuel (#49), missing
#   per-ship caps (#50), different ECA threshold (#51), different price avg (#52),
#   sailing-time pinned to v_max independent of speed var (#81).
#   They solve a different, easier problem.
# - CBLS rows use a different (also simplified) formulation: avg fuel coefficient
#   over fleet (#53), single per-cargo speed var with fleet-wide bounds (#54),
#   ECA constraint enforced only at >=0.99 (root cause of #57 noECA == ECA),
#   missing per-ship MGO tank cap (#78), time-window violated for unassigned
#   cargoes (#79).
# - No published BKS exists for these instances (synthetic factories in data.py).
# Until both sides agree (gates #56, #55), do NOT report a gap-vs-SCIP or
# gap-vs-BKS. CBLS rows are valid only as self-consistency / engine-throughput
# data points. See benchmarks/bunker-eca/FIDELITY.md for full audit.
# Post-port ViolationLS rows (epic #64 P0-P4; engine HEAD c0116c3, seed 42,
# default config, --verify PASS). Bunker is scalar-only (no List/Set vars) so the
# structural batch is inactive and the engine finds the same optima as CBLS-SA: a
# no-regression result. Still self-consistent only (simplified formulation, no
# BKS; see header + FIDELITY.md). noECA == ECA persists (#57).
# CBLS rows REMOVED (11 of 15): they were produced under the old
# 1e-9 feasibility tolerance, before the engine default moved to 1e-6, and this
# file records no tolerance so they cannot be interpreted. Regeneration is
# blocked on this benchmark having no committed generator (the runner does not
# write this file) - tracked in #103. Rows below are cited reference results
# and are unaffected. Removed rows remain in git history.
instance,method,objective,gap_pct,source
small-3s-10c,SCIP,1350768,,"PySCIPOpt MIQCP, 60s; BUGGY FORMULATION (#49-#52, #81) — not comparable"
medium-7s-30c,SCIP,,,"PySCIPOpt MIQCP no solution in 300s (and would be buggy if it did)"
large-15s-60c,SCIP,,,"PySCIPOpt MIQCP no solution in 600s (and would be buggy if it did)"
xlarge-30s-120c,SCIP,,,"PySCIPOpt MIQCP no solution in 1800s"
